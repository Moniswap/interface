/* eslint-disable react/display-name */
"use client";

import { forwardRef } from "react";

interface ModalProps {}

const SelectWalletModal = forwardRef<HTMLInputElement, {}>((props, ref) => {
  return <div></div>;
});

export default SelectWalletModal;
